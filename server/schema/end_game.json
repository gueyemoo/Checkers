{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "$id": "end_game",
  "type": "object",
  "allOf": [{"$ref": "/server/schema/command.json"}],
  "properties": {
    "user_id": {
      "type": "number",
      "description": "user id in database"
    },
    "winner": {
      "type": "number",
      "minimum": 1,
      "maximum": 2,
      "description": "user color"
    }
  },
  "required": ["user_id", "winner"]
}

